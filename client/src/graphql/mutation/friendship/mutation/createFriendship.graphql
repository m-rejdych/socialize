mutation CreateFriendship($addressedToId: ID!) {
  createFriendship(addressedToId: $addressedToId) {
    friendship {
      id
      isAccepted
    }
    profile {
      id
    }
    friendProfile {
      id
    }
  }
}
