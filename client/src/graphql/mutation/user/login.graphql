mutation Login($data: LoginInput!) {
  login(data: $data) {
    token
    user {
      id
      email
      firstName
      lastName
      fullName
      userName
      profile {
        id
        allFriends {
          id
        }
        acceptedFriends {
          id
        }
      }
    }
  }
}
