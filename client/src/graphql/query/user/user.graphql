query User($id: ID) {
  user(id: $id) {
    id
    email
    firstName
    lastName
    fullName
    userName
    isAdmin
    profile {
      id
      # allFriends {
      #   profile {
      #     id
      #   }
      #   friendship {
      #     id
      #     isAccepted
      #   }
      #   requestedByMe
      # }
      # acceptedFriends {
      #   profile {
      #     id
      #   }
      #   friendship {
      #     id
      #     isAccepted
      #   }
      # }
    }
  }
}
